{
  "name": "greta-native",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync-content": "node scripts/sync-content.mjs",
    "deploy:test": "npm run build && find dist/media -name '*.mp4' -delete && wrangler pages deploy dist --project-name=greta-native-test"
  },
  "dependencies": {
    "astro": "^5.1.1",
    "@astrojs/cloudflare": "^12.0.0",
    "gsap": "^3.12.5"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
